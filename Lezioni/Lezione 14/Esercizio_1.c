// Serve un puntatore nella seguente funzione int length(ListaDIElementi*l)?

int length(ListaDIElementi l)
{
    int n=0;
    while(l!=null)
    {
        n=n+1;
        return 1+lenght(ListaDiElementi l+1);
    }
}